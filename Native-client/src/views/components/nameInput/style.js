import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
    borderWidth: 1,
    borderRadius: 5,
    borderColor: 'black',
  },
  textInput: {
    fontSize: 20,
  },
});

export default styles;
